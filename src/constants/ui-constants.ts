import { DropDownOption } from '@thi.ng/hdom-components'

export const AUTOCORR_SCALES_OPT: [string, DropDownOption[]] = ['Scale AIC bars between',
    [
        ['abs', '0 and 1'],
        ['minmax', 'min and max']
    ]
]

